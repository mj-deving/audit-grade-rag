# Codex /goal prompt — audit-grade-rag Round 2: real-provider completion — 2026-05-12

You are Codex running in `/home/mj/projects/audit-grade-rag/`. This is a new `/goal` continuation after the previous goal completed. Treat repo state as the source of truth, not prior chat memory.

Read first, in this order:

- `AGENTS.md`
- `ISA.md`
- `docs/MASTER_PRD.md`
- `README.md`
- `package.json`
- `tests/integration-live/`

The existing `ISA.md` has the stable contract IDs: `ISC-1` through `ISC-51`. Existing `[x]` checkboxes are suspect unless the implementation satisfies the real-provider/product bar in the ISA and PRD. Do not call this a reference app, demo, skeleton, or showcase.

## Provider policy for Round 2

Use the repaired GoalMode rule: real dependency verification follows the contract, but local personal `/goal` verification should use the cheapest real path that exercises the dependency honestly.

- **Local LLM live verification default:** use the installed Claude Code CLI OAuth session via `claude -p --output-format json --json-schema ...` where a live LLM call is needed for local `/goal` evidence. Do not require `ANTHROPIC_API_KEY` merely to prove local live behavior if `claude -p` can exercise the same prompt/response contract.
- **Deployable provider abstraction:** keep production generation behind a provider interface. The product contract may support SDK/API providers and on-prem OpenAI-compatible vLLM, but local GoalMode L4 may use `claude -p` unless an ISC specifically demands SDK/API deployment behavior.
- **Commercial/server deployments:** do not hardcode the Claude OAuth CLI as the only production provider. It is acceptable as the local/personal provider adapter; deployable adapters must stay explicit and configurable.
- **Structured output:** for any Claude CLI live test that needs JSON, use `--json-schema` and parse the `structured_output` field from the CLI JSON envelope. Do not rely on prompt-only JSON formatting.
- **Replay honesty:** provider nondeterminism is handled by a structured `ReplayDriftError` naming the drifted artifact/provider output. Never silently let replay diverge.

Before editing implementation code, update any stale prompt/test/doc references that still require `@anthropic-ai/sdk` plus `ANTHROPIC_API_KEY` as the only LLM-live path. If an SDK/API credential remains required for a specific deployable-provider test, document why in `docs/MASTER_PRD.md` §13 and keep it separate from the local Claude OAuth L4 path.

## Goal

Implement `docs/MASTER_PRD.md` fully against `ISA.md`, closing every ISC against the real product bar. Continue from current repo state. Walk ISC groups in order and re-open any checkbox whose current implementation is only a deterministic stand-in.

## Execution rules

After each meaningful change:

1. Run `pnpm check:fast`.
2. Do not commit if it fails.
3. When an ISC is genuinely complete, flip the matching checkbox in BOTH `ISA.md` and `docs/MASTER_PRD.md` §9, preserving the `ISC-N` anchor.
4. Append one line to `docs/MASTER_PRD.md` §13: ISO timestamp, `ISC-N closed`, evidence path/test/commit.
5. Commit after each coherent ISC band or provider slice.

If the same approach fails three times on one ISC, append a `STUCK` note to PRD §13 naming the ISC, what failed, and what input is needed, then move to the next ISC.

## Product bars that must remain real

- ISC-1..6: auth/session/operator access is real, not bypassed.
- ISC-7..11: real Postgres + pgvector HNSW where the ISA requires it, not in-memory vector stubs.
- ISC-12..15: real hybrid BM25 + dense retrieval with RRF.
- ISC-16..20: real generation path through the provider abstraction; local live LLM proof may use Claude Code CLI OAuth with `--json-schema`, while deployable/API adapter behavior remains configurable.
- ISC-21..26: real hash-chained SQLite audit ledger with Ed25519 signatures.
- ISC-27..29: replay either reproduces byte-equality or returns structured `ReplayDriftError`; provider nondeterminism must be surfaced.
- ISC-30..34: real eval harness with committed attribution-cleared fixture corpus and enforced thresholds.
- ISC-35..38: real Typst-rendered EU AI Act §50 PDF, byte-deterministic on rerun.
- ISC-39..43: real operator console at `/console`, WCAG 2.2 AA, CSP `default-src 'self'`.
- ISC-44..47: DSGVO/compliance baseline; no PII/query leakage outside declared LLM provider.
- ISC-48..51: build/check/CI/readme contract is real.

## Live verification expectations

- `RUN_LIVE_TESTS=1 pnpm test:integration:live` must exercise every PRD §7.5 dependency that can run in this local environment.
- LLM live tests should use `claude -p` OAuth by default for local GoalMode evidence. If a test uses SDK/API credentials, it must fail loudly when the credential is absent and explain why the SDK/API path is required rather than the OAuth CLI path.
- Typst tests invoke the real `typst` binary.
- pgvector tests use real Postgres + pgvector, preferably docker-compose/local service as already configured.
- WebAuthn/Hono SSR tests exercise real libraries/routes.
- No live test may silently skip and still count as evidence.

## Done definition

All of the following must hold before declaring the goal complete:

- [ ] Every ISC in `ISA.md ## Criteria` is `[x]` against the real-provider/product bar.
- [ ] Every matching ISC in `docs/MASTER_PRD.md` §9 is `[x]`.
- [ ] `pnpm check:fast` exits 0.
- [ ] `RUN_LIVE_TESTS=1 pnpm test:integration:live` exits 0 using local Claude OAuth for LLM live proof unless an explicitly documented SDK/API test is required.
- [ ] `pnpm check:full` exits 0. If check:full intentionally does not run expensive live tests by default, document and run the live command separately before done.
- [ ] `pnpm build` exits 0.
- [ ] `bun ~/.claude/skills/GoalMode/Tools/ReconcileCheck.ts ISA.md docs/MASTER_PRD.md` exits 0.
- [ ] `bun ~/.claude/skills/GoalMode/Tools/PrdSpecificityGate.ts ISA.md docs/MASTER_PRD.md` exits 0.
- [ ] `docs/MASTER_PRD.md` §13 contains a completion report with commits and verification commands.
- [ ] README implementation status reflects actual provider wiring.

Begin by auditing current open/suspect ISC state and stale `ANTHROPIC_API_KEY`/`@anthropic-ai/sdk` assumptions, then work the next highest-leverage ISC band.
